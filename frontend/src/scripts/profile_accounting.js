import "../styles/profile.scss";

import * as Vue from 'vue/dist/vue.esm-browser';

let app = Vue.createApp({
    // compilerOptions: {
    //     delimiters: ["[[", "]]"]
    // },
    data() {
        return {
        }
    },
    methods: {
    }
});
app.mount("#app");